#include <stdio.h>

/**
 * main - A program that prints the name of the program
 * @argc: the total numbers of the arguments to the function
 * @argv: the array to the arguments passed to the program
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
